{
  "name": "redux-loop-router",
  "version": "0.0.0",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib dist",
    "build": "babel src --out-dir lib",
    "prepublish": "npm run clean && npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "6.8.0",
    "babel-preset-es2015": "6.6.0",
    "babel-preset-stage-0": "6.5.0",
    "rimraf": "2.3.4"
  }
}
